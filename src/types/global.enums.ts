export enum BrowserType {
  CHROMIUM = "chromium",
  FIREFOX = "firefox",
  WEBKIT = "webkit",
}

export enum FormatType {
  PDF = "pdf",
  XML = "xml",
  BOTH = "ambos",
}

export enum CepTypeStatus {
  PENDING = "pending",
  PROCESSING = "processing",
  COMPLETED = "completed",
  FAILED = "failed",
}
